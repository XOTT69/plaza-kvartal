rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ ТЕСТОВИЙ РЕЖИМ: будь-хто авторизований може читати/писати
    // Потім посилити правила!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}