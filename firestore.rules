rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ ТЕСТОВИЙ РЕЖИМ: всі можуть читати/писати без авторизації
    // Потім посилити!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}